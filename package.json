{
  "name": "reminderflow-bot",
  "version": "1.0.0",
  "description": "A production-ready Telegram Reminder Bot with group support",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run migrate && node src/index.js",
    "dev": "nodemon --ignore logs/ src/index.js",
    "migrate": "node src/database/migrate.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "moment": "^2.30.1",
    "moment-timezone": "^0.5.45",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0",
    "telegraf": "^4.16.3",
    "uuid": "^10.0.0",
    "winston": "^3.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
